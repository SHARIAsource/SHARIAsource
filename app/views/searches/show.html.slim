= content_for :page_title, "Search"

.inner-wrapper
  .inner
    h2.section-heading Advanced Search
    .search-two-columns
      .search-sidebar
        = render 'searches/filters'
      .search-results
        p Results
    .search-full-width
      = render 'searches/filters'
