= content_for :page_title, 'New Reference Type'
= content_for :admin_active, 'Metadata'

h2 New Reference Type

= render partial: 'admin/reference_types/form', locals: { form_url: admin_reference_types_path, :reference_type => ReferenceType.new }
